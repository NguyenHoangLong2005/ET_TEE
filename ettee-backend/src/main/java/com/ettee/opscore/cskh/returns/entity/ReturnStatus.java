package com.ettee.opscore.cskh.returns.entity;

/** Khớp enum Postgres: return_status */
public enum ReturnStatus {
    requested, approved, rejected, item_received, exchange_sent, completed, refunded, cancelled
}
